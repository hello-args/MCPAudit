# MCTS brand assets

Canonical logos for the Model Context Threat Scanner.

| File | Use |
|------|-----|
| `Logo 2.jpg` | Canonical logo — terminal header, HTML dashboard, docs |

Loaded in code via `mcts.brand.logo_data_uri()` and `mcts.brand.LOGO_PATH`.
