name: MCTS Security Scan
description: Scan an MCP server for security vulnerabilities
branding:
  icon: shield
  color: red

inputs:
  target:
    description: Path to MCP server entrypoint
    required: true
    default: ./server.py
  fail-on-critical:
    description: Fail the workflow if critical findings exist
    required: false
    default: "true"

runs:
  using: composite
  steps:
    - name: Install MCTS
      shell: bash
      run: pip install mcts

    - name: Run security scan
      shell: bash
      run: |
        ARGS="${{ inputs.target }}"
        if [ "${{ inputs.fail-on-critical }}" = "true" ]; then
          mcts scan "$ARGS" --fail-on-critical -o mcts-report.json
        else
          mcts scan "$ARGS" -o mcts-report.json
        fi

    - name: Upload report
      uses: actions/upload-artifact@v4
      with:
        name: mcts-report
        path: mcts-report.json
