name: MCPVault Security Scan
description: Scan an MCP server for security vulnerabilities
branding:
  icon: shield
  color: red

inputs:
  target:
    description: Path to MCP server entrypoint
    required: true
    default: ./server.py
  fail-on-critical:
    description: Fail the workflow if critical findings exist
    required: false
    default: "true"

runs:
  using: composite
  steps:
    - name: Install MCPVault
      shell: bash
      run: pip install mcpvault

    - name: Run security scan
      shell: bash
      run: |
        ARGS="${{ inputs.target }}"
        if [ "${{ inputs.fail-on-critical }}" = "true" ]; then
          mcpvault scan "$ARGS" --fail-on-critical -o mcpvault-report.json
        else
          mcpvault scan "$ARGS" -o mcpvault-report.json
        fi

    - name: Upload report
      uses: actions/upload-artifact@v4
      with:
        name: mcpvault-report
        path: mcpvault-report.json
